# App screenshots
Drop screenshots here using the `image` path defined in `src/data/apps.js`,
for example `portfolio.png` for the `/images/apps/portfolio.png` entry.
Any missing file falls back to a generated gradient cover.
Recommended: 1600x900 (16:9), PNG or WebP, under ~300 kB.
